# Phase113 train-only execution; holdout prohibited.
name: V99 R106 Phase113 premium crowding reversal train alpha
on:
  push:
    branches: [research/v99-r106-native-all-regime-engine]
    paths: ['scripts/run_v99_r106_phase113_premium_crowding_reversal_train_alpha.py','.github/workflows/v99-r106-phase113-premium-crowding-reversal-train-alpha.yml']
  workflow_dispatch:
permissions: {contents: write}
concurrency: {group: v99-r106-phase113-premium-crowding-reversal, cancel-in-progress: false}
jobs:
  train-alpha:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    env: {PYTHONUNBUFFERED: "1", PIP_DISABLE_PIP_VERSION_CHECK: "1", RESEARCH_BRANCH: research/v99-r106-native-all-regime-engine}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with: {ref: research/v99-r106-native-all-regime-engine, fetch-depth: 0, persist-credentials: true}
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with: {python-version: "3.12", cache: pip}
      - id: cache_date
        shell: bash
        run: echo "date=$(date -u +%F)" >> "$GITHUB_OUTPUT"
      - uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830
        with:
          path: data/canonical
          key: Linux-paper-v15-market-v1-${{ steps.cache_date.outputs.date }}
          restore-keys: |
            Linux-paper-v15-market-v1-
            Linux-paper-v13-market-
      - run: python -m pip install -e ".[dev]"
      - name: Restore canonical V15 state
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin paper-results
          for path in state/paper_v15_universe.json reports/paper_data_sync_v15.json; do git cat-file -e "FETCH_HEAD:$path"; mkdir -p "$(dirname "$path")"; git show "FETCH_HEAD:$path" > "$path"; done
      - name: Prepare canonical V15 replay
        run: python scripts/prepare_dashboard_v15_replay.py
      - name: Snapshot frozen assets
        run: |
          python - <<'PY'
          import hashlib,json,pathlib
          files=sorted({p for pat in ('*v16*frozen*','*v99*frozen*') for p in pathlib.Path('.').rglob(pat) if p.is_file() and '.git' not in p.parts})
          pathlib.Path('/tmp/frozen_before.json').write_text(json.dumps({str(p):hashlib.sha256(p.read_bytes()).hexdigest() for p in files},sort_keys=True))
          PY
      - name: Run Phase113 train-only gate
        run: python scripts/run_v99_r106_phase113_premium_crowding_reversal_train_alpha.py
      - name: Verify frozen assets unchanged
        if: always()
        run: |
          python - <<'PY'
          import hashlib,json,pathlib
          p=pathlib.Path('/tmp/frozen_before.json');assert p.exists(),'Frozen snapshot missing'
          before=json.loads(p.read_text())
          after={x:hashlib.sha256(pathlib.Path(x).read_bytes()).hexdigest() for x in before}
          assert before==after,'Frozen asset mutation detected'
          print('V16/V99 frozen guards OK')
          PY
      - name: Persist evidence
        if: success()
        shell: bash
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          r=json.loads(Path('reports/candidate_v99_r106_phase113_premium_crowding_reversal_train_alpha.json').read_text())
          p=r['precommitment']
          assert r['frozen_assets_untouched']['v16'] and r['frozen_assets_untouched']['v99_frozen']
          assert p['holdout_not_downloaded_or_parsed'] and p['single_hypothesis_no_grid'] and p['causal_t_minus_1']
          assert p['rolling_hours_fixed']==8 and p['min_crosssection_assets']==10 and p['cost_for_selection']=='severe'
          assert r['data_integrity']['january_2024_monthly_archive_requested'] is False
          assert r['status'] in ('TRAIN_ALPHA_PASS_FREEZE_FOR_DOWNSTREAM','TRAIN_ALPHA_REJECT')
          PY
          git config user.name "github-actions[bot]";git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/candidate_v99_r106_phase113_premium_crowding_reversal_train_alpha.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Record V99 R106 Phase113 train-only premium crowding gate"
          git fetch origin ${RESEARCH_BRANCH};git rebase origin/${RESEARCH_BRANCH};git push origin HEAD:${RESEARCH_BRANCH}
