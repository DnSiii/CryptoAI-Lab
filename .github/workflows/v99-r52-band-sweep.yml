name: CryptoAI V99 R52 narrow band sweep
on:
  workflow_dispatch:
  push:
    branches: [research/v99-r52-band-sweep]
    paths:
      - "scripts/run_v99_r52_band_sweep.py"
      - "scripts/run_v99_r47_band_rebalanced_crash_shield.py"
      - "scripts/run_v99_r37_crash_shield.py"
      - "scripts/run_v99_r25_trisleeve_meta.py"
      - ".github/workflows/v99-r52-band-sweep.yml"
permissions:
  contents: write
concurrency:
  group: v99-r52-band-sweep
  cancel-in-progress: true
jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 240
    env:
      PYTHONUNBUFFERED: "1"
      PIP_DISABLE_PIP_VERSION_CHECK: "1"
    steps:
      - name: Check out research branch
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0
      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
          cache: pip
      - name: Resolve UTC cache date
        id: utc
        shell: bash
        run: echo "date=$(date -u +%F)" >> "$GITHUB_OUTPUT"
      - name: Restore canonical market cache
        uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830
        with:
          path: data/canonical
          key: ${{ runner.os }}-paper-v15-market-v1-${{ steps.utc.outputs.date }}
          restore-keys: |
            ${{ runner.os }}-paper-v15-market-v1-
            ${{ runner.os }}-paper-v13-market-
      - name: Install package
        run: python -m pip install -e "."
      - name: Restore published replay state
        shell: bash
        run: |
          git fetch --no-tags origin paper-results
          for path in state/paper_v15_universe.json reports/paper_data_sync_v13.json reports/paper_data_sync_v15.json; do
            git cat-file -e "FETCH_HEAD:$path" || exit 1
            mkdir -p "$(dirname "$path")"
            git show "FETCH_HEAD:$path" > "$path"
          done
      - name: Prepare frozen V15 replay inputs
        run: python scripts/prepare_dashboard_v15_replay.py
      - name: Run V99 R52 1pp 2pp 3pp band sweep
        run: python scripts/run_v99_r52_band_sweep.py
      - name: Upload V99 R52 evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: v99-r52-band-sweep-${{ github.run_id }}
          retention-days: 60
          path: |
            reports/candidate_v99_r52_band_1pp.json
            reports/candidate_v99_r52_band_2pp.json
            reports/candidate_v99_r52_band_3pp.json
            reports/candidate_v99_r52_band_sweep.json
      - name: Persist V99 R52 reports
        shell: bash
        run: |
          test -f reports/candidate_v99_r52_band_sweep.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/candidate_v99_r52_band_*.json
          if git diff --cached --quiet; then
            echo "Reports unchanged"
          else
            git commit -m "Persist V99 R52 narrow band sweep"
            git push origin HEAD:research/v99-r52-band-sweep
          fi
